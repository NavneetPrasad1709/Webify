/* Canonical public origin. The apex host 308-redirects to www, so every
   absolute URL the code emits must use the www host. */
export const SITE_URL = "https://www.webify.org.in";

/* Cal.com intro-call booking page. Kept here so a Cal username change is a
   one-line edit: the handle is still Cal's auto-generated one. */
export const BOOKING_URL = "https://cal.com/navneet-prasad-geein6/intro";
