# CLAUDE.md

Read and follow all instructions in `./AGENTS.md` — that is the single source of truth for this project (tech stack, commands, and the full audit checklist).

Claude Code-specific notes:
- You have full filesystem + terminal access here under the Claude Max plan — use it. Actually run `npm run build` and `npm run lint` after changes, don't just claim they'd pass.
- When asked for a "full audit," work through AGENTS.md section by section and produce the report before making changes, unless told to fix directly.
- Keep this file thin — do not add project rules here. Add them to `AGENTS.md` so Antigravity and any other tool stay in sync.
